{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "pnpm install --frozen-lockfile",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/cron/google-calendar-renew",
      "schedule": "0 3 * * *"
    }
  ]
}
