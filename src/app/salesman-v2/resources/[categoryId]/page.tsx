import Page from '@/app/salesman/resources/[categoryId]/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function SalesmanV2ResourcesCategoryIdPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
