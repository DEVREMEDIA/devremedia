import Page from '@/app/salesman/leads/[leadId]/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function SalesmanV2LeadsLeadIdPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
