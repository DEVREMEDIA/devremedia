import Page from '@/app/salesman/leads/[leadId]/edit/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function SalesmanV2LeadsLeadIdEditPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
