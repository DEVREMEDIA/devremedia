import Page from '@/app/admin/proposals/[proposalId]/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function AdminV2ProposalsProposalIdPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
