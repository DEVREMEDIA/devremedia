import Page from '@/app/admin/filming-prep/[projectId]/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function AdminV2FilmingPrepProjectIdPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
