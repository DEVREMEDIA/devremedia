import Page from '@/app/admin/clients/[clientId]/edit/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function AdminV2ClientsClientIdEditPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
