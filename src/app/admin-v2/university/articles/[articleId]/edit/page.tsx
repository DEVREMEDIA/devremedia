import Page from '@/app/admin/university/articles/[articleId]/edit/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function AdminV2UniversityArticlesArticleIdEditPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
