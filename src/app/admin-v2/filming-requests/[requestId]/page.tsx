import Page from '@/app/admin/filming-requests/[requestId]/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function AdminV2FilmingRequestsRequestIdPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
