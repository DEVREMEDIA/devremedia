import Page from '@/app/admin/contracts/[contractId]/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function AdminV2ContractsContractIdPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
