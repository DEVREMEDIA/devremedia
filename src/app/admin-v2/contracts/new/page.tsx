import Page from '@/app/admin/contracts/new/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function AdminV2ContractsNewPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
