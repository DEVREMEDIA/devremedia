import Page from '@/app/admin/projects/[projectId]/edit/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function AdminV2ProjectsProjectIdEditPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
