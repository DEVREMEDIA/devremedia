import Page from '@/app/client/invoices/[invoiceId]/success/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function ClientV2InvoicesInvoiceIdSuccessPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
