import Page from '@/app/client/invoices/[invoiceId]/cancel/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function ClientV2InvoicesInvoiceIdCancelPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
