import Page from '@/app/client/contracts/[contractId]/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function ClientV2ContractsContractIdPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
