import Page from '@/app/client/contracts/[contractId]/sign/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function ClientV2ContractsContractIdSignPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
