import Page from '@/app/employee/university/[categorySlug]/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function EmployeeV2UniversityCategorySlugPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
