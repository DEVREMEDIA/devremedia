import Page from '@/app/employee/deliverables/[projectId]/page';

/** Ίδια σελίδα, μέσα στο νέο κέλυφος. */
export default function EmployeeV2DeliverablesProjectIdPage(props: Parameters<typeof Page>[0]) {
  return <Page {...props} />;
}
